services:
  api:
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./app:/app
    ports:
      - "8000:8000"
    environment:
      DEBUG: "1"
      # DJANGO_SETTINGS_MODULE: config.settings.dev